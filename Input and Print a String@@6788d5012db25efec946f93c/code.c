#include <stdio.h>

int main() {
    char str[25];
    
    // Take input using scanf
    scanf("%s", &str);  // Using "%c" to read a single character
    
    // Print the entered character
    printf("You entered: %s\n", str);  // Using "
    return 0;
}