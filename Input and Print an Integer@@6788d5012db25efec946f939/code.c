#include <stdio.h>

int main() {
    int x;  
    
    // Take input using scanf
    scanf("%d", &x);  // Using "%c" to read a single character
    
    // Print the entered character
    printf("You entered: %d\n", x);  // Using "%c" to print the character

    return 0;
}